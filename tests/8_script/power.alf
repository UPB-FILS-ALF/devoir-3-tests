/* Power function */

function power (integer a, integer n):integer 		
begin
	declare integer p=1;
	for i from 1 to n start
		p = p * a;
	finish;
	return a;
end;

declare n = 0;

n = exec power (6, 5);