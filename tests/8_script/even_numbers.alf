/* Even numbers from 1 to 1000 */

declare n = 2;

for n from 2 to 1000 start
	exec _write (text=n);
finish;

while n < 1000 start
	exec _write (text=n);
	n = n + 2;
finish;

n = 2;

repeat
	exec _write (text=n);
	n = n + 2;
while n < 1000;